import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function ListingScreen({ user, setUser }) {
  return (
    <View>
      <Text>You are now logged in</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
